[credential]
	helper = store
[user]
	email = rakennusmf@gmail.com
	name = rakennus
[init]
	defaultBranch = master
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
